/**
 * Where the curious end up.
 *
 * Shared so the wordmark and the "?" beside it can never drift apart — they
 * are the same promise made twice, and the joke only works if both keep it.
 */
export const ABOUT_URL = "https://www.youtube.com/watch?v=dQw4w9WgXcQ";
