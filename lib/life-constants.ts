/** Where a new life calendar starts — a typical lifespan, not a limit. */
export const LIFE_SPAN_DEFAULT = 85;

/** Hard ceiling for the setting (and the grid): nobody plots past this. */
export const LIFE_SPAN_MAX = 120;
