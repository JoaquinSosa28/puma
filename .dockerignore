# Rebuilt inside the image
node_modules
.next
out
build
coverage

# VCS / CI
.git
.github

# Local env & secrets (never bake into the image)
.env
.env.*
!.env.example

# Tooling / cruft
Dockerfile
.dockerignore
*.tsbuildinfo
.DS_Store
npm-debug.log*
.vitest

# Docs / prototype assets not needed at build time
prototype
**/*.dc.html
